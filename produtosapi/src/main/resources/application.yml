spring:
  application:
    name: Produtos API
  datasource:
    url: jdbc:h2:mem:meubanco
    username: produtos
    password: password

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console
